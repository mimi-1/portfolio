import * as React from "react"
import PropTypes from "prop-types"
import { useStaticQuery, graphql } from "gatsby"

import "./sidebar.scss"

const Sidebar = props => {
  return <section className="sidebar">This is a vertical sidebar</section>
}

export default Sidebar
